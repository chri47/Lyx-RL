[phases.setup]
nixPkgs = ['nodejs-18_x', 'python3', 'cairo', 'pango', 'libpng', 'jpeg', 'giflib', 'librsvg', 'pkg-config', 'build-essential']

[phases.build]
cmds = ['npm install --build-from-source=canvas']

[start]
cmd = 'npm start'
